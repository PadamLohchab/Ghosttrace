# GhostTrace — Requirements
# Install with: pip install -r requirements.txt

# ── GUI ────────────────────────────────────────────────────
PyQt6>=6.6.0
PyQt6-Qt6-Svg>=6.6.0            # Required for GeoMap SVG rendering (geo_panel.py)

# ── AI Backends (install the one you need) ─────────────────
anthropic>=0.25.0               # For AI_BACKEND=anthropic
# google-genai>=0.8.0           # For AI_BACKEND=gemini  (pip install google-genai)

# ── Environment Variables ───────────────────────────────────
python-dotenv>=1.0.0

# ── Logging ─────────────────────────────────────────────────
loguru>=0.7.0

# ── Forensics Libraries (some are optional on Windows) ──────
# pytsk3>=20230125              # Disk image parsing — OPTIONAL, hard to build on Windows
# python-registry>=1.3.1       # Windows registry parsing — OPTIONAL

construct>=2.10.68              # Binary format parsing

# ── Network / PCAP ─────────────────────────────────────────
scapy>=2.5.0
dpkt>=1.9.8

# ── File Analysis ───────────────────────────────────────────
# python-magic>=0.4.27          # Requires libmagic.dll on Windows — OPTIONAL
pefile>=2023.2.7                # PE executable analysis (pure Python, works on Windows)

# ── PDF Report Generation ───────────────────────────────────
fpdf2>=2.7.9

# ── Data Handling ───────────────────────────────────────────
pandas>=2.1.0

# ── Threat Intel / Network ─────────────────────────────────
requests>=2.31.0

# ── Geolocation ─────────────────────────────────────────────
# geoip2>=4.7.0                 # OPTIONAL — app uses ip-api.com (free, no key) by default

# ── Visualization ───────────────────────────────────────────
matplotlib>=3.8.0

# ── JSON Export ─────────────────────────────────────────────
# Built-in json module used — no extra dependency needed

# ── Optional: Volatility3 for memory forensics ─────────────
# volatility3>=2.5.0
